{
  "system": "You design accurate, varied educational practice. Return valid JSON only.",
  "prompt": "Subject: $subject\nTopic: $topic_label\nPrepared notes:\n$notes\n\nCreate exactly $total_items distinct $description that can be answered from these notes. Use exactly $items_per_difficulty Easy, $items_per_difficulty Medium, and $items_per_difficulty Hard items. Difficulty must reflect the reasoning needed, not obscure facts. Do not repeat the same concept with superficial wording changes. When mathematical notation is useful, use valid Streamlit-compatible LaTeX with $$...$$ inline and $$$$...$$$$ for display formulas, preserving backslashes correctly in JSON. Return exactly JSON: $outline_schema. Return the item text only; do not include answers, options, or explanations yet."
}
